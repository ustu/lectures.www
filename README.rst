Лекции
======

Инициализация:

.. code-block:: bash

    $ git submodule update --init --recursive

Сборка:

.. code-block:: bash

    $ vagrant up --provider=docker

Пересборка:

.. code-block:: bash

    $ vagrant reload

Запуск:

.. code-block:: bash

    $ firefox build/html/index.html
